# These run on Linux during the .deb build — they must keep LF line endings,
# otherwise bash/dpkg choke on the carriage returns.
*.sh text eol=lf
*.desktop text eol=lf
ClariFi-linux.spec text eol=lf
